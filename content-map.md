# Rhein Portfolio Content Map

这个模板适合先保留视觉结构，再逐步替换真实作品集内容。

## 首页
- `index.html` 的 hero 区：替换姓名、定位、一句话介绍和邮箱。
- `.hero-panel`：替换成你的真实经验标签，例如项目年限、数据后台、B 端系统、G 端业务。
- `.standards`：替换成你常用的 UX 判断标准，例如可用性测试、权限可解释、异常处理、数据可追溯。

## 项目卡片
- `.case-card` 每个就是一个项目入口。
- 替换 `<img>` 为真实项目封面图。
- 替换 `.case-meta` 为项目类型。
- 替换 `<h3>` 为项目标题。
- 替换描述段落为项目一句话成果。
- 替换 `Complexity` 和 `UX Move` 为项目复杂点与设计动作。

## Data Console
- `#prototype` 适合放数据后台或 B 端系统的核心截图、Figma 原型、录屏链接或任务流程。
- `<pre><code>` 里的任务流可替换成脱敏后的真实业务流程。
- `.diagnostic-pane` 适合展示权限校验、任务定位、指标口径、异常反馈等设计亮点。

## Process / Matrix
- `#process` 写你的通用设计方法。
- `#matrix` 写每个项目展开时的叙事骨架：问题、研究、决策、结果。

## 推荐准备的真实素材
- 3 张项目封面图。
- 每个项目 1 张流程图。
- 每个项目 2 到 4 张关键界面。
- 每个项目 2 个真实或可脱敏的结果指标。
- 一段 80 到 120 字的个人 UX 介绍。
